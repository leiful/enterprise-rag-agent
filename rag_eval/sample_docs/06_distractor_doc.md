# Distractor Document

This document is used to test whether the RAG system incorrectly treats similar keywords as evidence.

SQLite can store lightweight application data such as users, sessions, configuration, logs, and small indexes. SQLite is not a dedicated vector database for million-scale high-dimensional semantic search.

Streaming text chat is not the same as a real-time voice call. Streaming chat usually means text is returned progressively, while a real-time voice call requires bidirectional low-latency audio capture, encoding, transport, decoding, and playback.

Conversation history can be used as raw material for a long-term memory system, but raw history alone is not enough. Long-term memory usually needs extraction, summarization, structured storage, retrieval, and forgetting rules.

